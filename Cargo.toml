[package]
name = "rapier"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1.0"
chrono = { version = "0.4.42", features = ["serde"] }
codes-iso-3166 = "0.1.5"

clap = { version = "4.5.48", default-features = false, optional = true }

[features]
default = ["youtube"]
youtube = []
clap-derive = ["clap/derive"]
